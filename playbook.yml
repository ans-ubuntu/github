---
- name: Deploy Web UI container
  hosts: remote_host
  become: yes
  tasks:
    - name: Deploy WebUI container
      docker_container:
        name: webui
        image: deatend/webui:latest
        state: started
        recreate: yes
        ports:
          - "8080:8080"













  
