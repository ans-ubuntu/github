all:
  hosts:
    remote_host:
      ansible_host: 178.128.83.86
      ansible_user: ans
      ansible_become: yes
      ansible_become_method: sudo
      ansible_become_password: ${{ SSH_BECOME_PASSWD }}
